server {
    listen 8080;

    root ./www;

    location / {
        allow_methods GET HEAD;
    }

    location /put_test {
        allow_methods PUT;
        # upload_directory

        client_max_body_size 100;
        test
        test
        root ./YoupiBanane;
        allow_methods POST;
    }

    # location * .bla {
    #   allow_methods POST;
    # }

    location /directory/ {
        allow_methods GET POST;
        root ./YoupiBanane;
        index youpi.bad_extension;
    }
#    Test;
}