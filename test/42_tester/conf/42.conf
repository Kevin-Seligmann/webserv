server {
    listen 8080;

    root ./www;

    location / {
        autoindex on;
        allow_methods GET;
    }

    location /put_test {
        # allow_methods PUT;
        # upload_directory
    }

    location /post_body {
        allow_methods POST;
        #max_body ?
    }

    # location * .bla {
    #   allow_methods POST;
    #   cgi_path ?
    # }

    location /directory {
        allow_methods GET;
        root ./YoupiBanane;
        index youpi.bad_extension;
    }
}