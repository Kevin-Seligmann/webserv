server {
    listen 8080;

    root ./www;

    location / {
        allow_methods GET;
    }

    location /put_test {
        # allow_methods PUT;
        # upload_directory
    }

    location /post_body {
        allow_methods POST;
    }

    # location * .bla {
    #   allow_methods POST;
    # }

    location /directory/ {
        allow_methods GET;
        root ./YoupiBanane;
        index youpi.bad_extension
    }
}